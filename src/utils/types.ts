// NOTE: Types doesn't feels in right location, research and change

export interface PaymentInfo {
    currency_name: string;
    payment_id: string;
    payment_address: string;
}

export interface AccessToken {
    access_token: string;
}
